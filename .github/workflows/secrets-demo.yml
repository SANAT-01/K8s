name: Secrets Demo

on:
  workflow_dispatch:

jobs:
  echo-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a secret
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
        run: |
          echo "The secret is: $MY_SECRET"
